# INTERFACE REFERENCE LOCK

## Canonical Visual Authority
The approved Culture Coalition Cloud Infinity Command Center visual is the interface authority.

Recognized local master names include:
- `ccc_infinity_visual_master.png`
- `Culture Coalition Cloud: Infinity Command Center.png`
- `Culture Coalition Cloud Command Center.png`
- `Culture Coalition Cloud Command Center(1).png`

## Architectural Rule
The image is the interface.

Not acceptable:
- generic dashboard plus image background
- substitute reconstruction
- random new HUD panels that break the approved geometry
- visual PASS based on resemblance alone

Required:
- exact approved reference bytes
- SHA-256 identity lock
- exact/relative coordinate map tied to the canonical frame
- transparent interaction regions over the original geometry
- live/read-only values placed inside the original visual language
- process/server/host/port/PID evidence
- machine PASS receipt
- final Human Command visual ratification

## Gate Relationship
`INTERFACE_MATCH` is upstream of `MAP_LOCK`, `KEYCHAIN_LOCK`, `DEVICE_VERIFY`, `REVENUE_READY`, and `HUMAN_RELEASE`.

Technical runtime verification may move `INTERFACE_MATCH` from HOLD to REVIEW. Only final visual ratification may move it from REVIEW to PASS.

## Existing HP/Crostini Runtime Evidence
The prior canonical interface work uses the local runtime root:

`$HOME/CCC_LIVING_OS/00_HOME/CCC_INFINITY_VISUAL_MASTER`

Expected machine state file:

`$HOME/CCC_LIVING_OS/00_HOME/CCC_INFINITY_VISUAL_MASTER/state/current.json`

The gate checker in this module reads that evidence without mutating the interface runtime.

## No Doctrine Leakage
This module contains technical interface verification criteria only. It does not publish or inject Unc's World private doctrine into CCC.

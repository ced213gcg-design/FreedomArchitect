# CCC Ledger v10

Phase-1 Ledger is a local append-only, hash-linked JSONL evidence chain. It is deliberately not a blockchain by default. Every canonical event preserves source, time, owner, change, validation, provenance, synthetic classification, and artifact hashes.

Ledger does not authorize privileged execution. It records evidence and state transitions.

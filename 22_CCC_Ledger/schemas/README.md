# Ledger Schema Pointer

The canonical Ledger event contract is `20_CCC_Living_Organism/schemas/ledger-event.schema.json`. v10 deliberately keeps one authoritative schema rather than forking copies that can drift.

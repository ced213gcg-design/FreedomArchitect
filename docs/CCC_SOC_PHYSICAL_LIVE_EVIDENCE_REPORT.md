# CCC SOC Physical Live Evidence Report

## FACT

- Repository: `ced213gcg-design/FreedomArchitect`
- Mission branch: `hotfix/ccc-soc-five-live-evidence-v1`
- PR: #4, draft and unmerged.
- Software P0 was green before this physical-evidence taper.
- Existing `SOC-01..SOC-05` are now explicitly **SOC CONTROL GATES**.
- New `DET-01..DET-05` are explicitly **SOC DETECTION SCENARIOS**.
- No physical scenario is marked PASS from configuration or software presence.

## EVIDENCE

Software evidence is provided by GitHub Actions and the repository test suite. Physical evidence must be generated by the HP and Dell one-drop launchers and the bounded guest-local trigger helpers.

## VALUE

The patch separates two questions that must never be conflated:

1. **Control Gates:** can the CCC SOC pipeline carry trustworthy evidence?
2. **Detection Scenarios:** can registered sensors observe five bounded security behaviors and reconcile source -> detection -> Ledger -> Dashboard?

This makes the demo useful as evidence instead of merely visually persuasive.

## RISK

- Physical HP rendering is not yet proven by this report.
- Dell live-state freshness is not yet proven by this report.
- No detection scenario has three real correlated runs yet.
- `CCC-SOC-BLUE` intentionally has no registered range IP in the current authoritative topology. DET-02 therefore remains `HOLD_MISSING_REGISTERED_BLUE_RANGE_TARGET` until an explicit authorized target is registered; no address is invented.
- DET-04 depends on a real writable Wazuh/syscheck monitored path.
- DET-05 depends on existing process telemetry. Missing telemetry produces HOLD, not synthetic evidence.
- Cross-host latency must be treated as `VERIFY_CLOCK_SKEW` if timestamps are materially inconsistent.

## ACTION

1. Run `scripts/warroom/CCC_HP_SOC_WARROOM_ONE_DROP.sh` on the HP/Crostini host.
2. Run `scripts/warroom/CCC_DELL_SOC_WARROOM_ONE_DROP.ps1` in Windows PowerShell 5.1 as Administrator on the Dell.
3. Verify HTTP evidence transport or use the sanitized `FILE_FALLBACK` payload.
4. Execute bounded guest-local triggers one scenario at a time.
5. Reconcile trigger, source, sensor, detection, Ledger and dashboard timestamps.
6. Review hashes and sanitized evidence before any report promotion.

## CURRENT BRANCH / SHA

Branch: `hotfix/ccc-soc-five-live-evidence-v1`

SHA: updated by implementation; see PR #4 exact head and session manifest at physical execution time.

## HP PHYSICAL RESULT

`NOT_YET_EXECUTED`

## DELL PHYSICAL RESULT

`NOT_YET_EXECUTED`

## CONTROL GATES

- SOC-01 Range Reachability: `UNKNOWN` until live evidence
- SOC-02 Sensor Visibility: `UNKNOWN` until live evidence
- SOC-03 Detection Pipeline: `UNKNOWN` until live evidence
- SOC-04 Honeypot Evidence: `UNKNOWN` until live evidence
- SOC-05 Evidence Reconciliation: `UNKNOWN` until live evidence

## DETECTION SCENARIOS

- DET-01 Network Recon: `UNKNOWN`
- DET-02 Failed SSH Auth: `HOLD` pending explicit registered Blue range target
- DET-03 Cowrie Interaction: `UNKNOWN`
- DET-04 File Integrity Change: `UNKNOWN` pending monitored-path preflight
- DET-05 Benign Process / Command: `UNKNOWN` pending existing process-telemetry preflight

## RUN COUNTS

Target: 15 real run records, 3 per scenario.

Current physical count: `0` in this pre-deployment report.

## DETECTION LATENCY

`NOT_YET_PROVEN`

Required measurements:

- trigger_to_sensor_ms
- sensor_to_detection_ms
- detection_to_ledger_ms
- ledger_to_dashboard_ms
- end_to_end_ms

## SENSOR INVENTORY

Generated at runtime as `artifacts/soc-live-five/sensor-inventory.json` on the HP evidence workspace and `C:\CCC\Evidence\warroom\<run-id>\sensor-inventory.json` on Dell.

## FAILURES

None may be inferred before physical execution. Runtime failures must be recorded without state inflation.

## HOLDS

- HP physical review outstanding.
- Dell physical review outstanding.
- DET-02 registered Blue range target outstanding.
- Scenario sensor availability remains unproven until physical preflight.

## CLOCK SKEW

`NOT_YET_MEASURED`

Material reverse timestamp ordering produces `VERIFY_CLOCK_SKEW` and suppresses false latency precision.

## SECURITY / AUTHORITY

- Evidence bridge is Dell -> HP only.
- HTTP mode requires a per-session bearer token plus HMAC SHA-256 over the exact payload bytes.
- Payload schema rejects unknown top-level fields and forbidden secret/command fields.
- Maximum payload is 1 MiB.
- No browser-to-PowerShell command channel exists.
- Guest scenario helpers are manually invoked, bounded and allowlist/role checked.
- Runtime evidence and tokens are ignored by Git.

## EVIDENCE HASHES

`NOT_YET_GENERATED`

Dell one-drop produces `sha256sums.txt` and `evidence-index.json` for the physical package.

## WHAT IS PROVEN

- Software architecture can represent separate Control Gates and Detection Scenarios.
- No-evidence state remains UNKNOWN/HOLD rather than PASS.
- The evidence bridge contract, one-drop launchers and bounded scenario helpers exist in code and must pass CI before physical use.

## WHAT IS NOT PROVEN

- Physical HP dashboard behavior.
- Physical Dell live-state freshness.
- Actual sensor availability inside guests.
- Real detection correlation.
- Repeatability.
- End-to-end latency.
- Human visual acceptance.

## NEXT ACTION

Run the HP one-drop, then the Dell one-drop, then execute and reconcile the bounded scenarios inside the authorized CCC lab.

## STATE

HOLD_WITH_DEFECTS

HOLD_WITH_DEFECTS

window.CCCSphere={draw(canvas,payload){const ctx=canvas.getContext('2d');if(!ctx){return false}ctx.clearRect(0,0,canvas.width,canvas.height);const nodes=payload.nodes||[],edges=payload.edges||[],cx=canvas.width/2,cy=canvas.height/2,pos={};nodes.forEach((n,i)=>{const a=(i/Math.max(1,nodes.length))*Math.PI*2+((n.x||0)*0.04);const radius=100+(n.y||0)*1.8;pos[n.id]={x:cx+Math.cos(a)*radius,y:cy+Math.sin(a)*radius*.55}});edges.forEach(e=>{const a=pos[e.source],b=pos[e.target];if(!a||!b)return;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.globalAlpha=.18;ctx.lineWidth=1+((e.volume||0)/50);ctx.strokeStyle='#93c5fd';ctx.stroke();ctx.globalAlpha=1;ctx.lineWidth=1});nodes.forEach(n=>{const p=pos[n.id],size=5+((n.w||n.importance||20)/12),activity=Math.min(20,n.activity||0);ctx.beginPath();ctx.arc(p.x,p.y,size+activity*.08,0,Math.PI*2);ctx.globalAlpha=.30+.007*(n.s||n.confidence||0);ctx.fillStyle=n.state==='VERIFY'?'#fbbf24':n.state==='READY'?'#34d399':n.state==='BUILD'?'#60a5fa':n.state==='ISOLATE'?'#f87171':'#94a3b8';ctx.fill();ctx.globalAlpha=1;ctx.fillStyle='#e5e7eb';ctx.font='12px sans-serif';ctx.fillText(n.label,p.x+size+4,p.y+4)});return true}}

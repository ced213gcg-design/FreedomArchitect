window.CCCSphere={draw(canvas,payload){const ctx=canvas.getContext('2d');if(!ctx){return false}ctx.clearRect(0,0,canvas.width,canvas.height);const nodes=payload.nodes||[];const cx=canvas.width/2,cy=canvas.height/2;nodes.forEach((n,i)=>{const a=(i/Math.max(1,nodes.length))*Math.PI*2;const radius=130+(n.y||0)*1.5;const x=cx+Math.cos(a)*radius,y=cy+Math.sin(a)*radius*.55;const size=5+(n.importance||20)/12;ctx.beginPath();ctx.arc(x,y,size,0,Math.PI*2);ctx.globalAlpha=.35+.006*(n.confidence||0);ctx.fillStyle=n.state==='VERIFY'?'#fbbf24':n.state==='READY'?'#34d399':n.state==='BUILD'?'#60a5fa':'#94a3b8';ctx.fill();ctx.globalAlpha=1;ctx.fillStyle='#e5e7eb';ctx.font='12px sans-serif';ctx.fillText(n.label,x+size+4,y+4)});return true}}
